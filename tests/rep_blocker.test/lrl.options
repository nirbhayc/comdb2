# turn off libevent to avoid ocassional "failed with rc 240 Master
# node downgrading - new master will resume schemachange"
libevent off
# dump sql statements blocking replication for more than a second
dump_sql_on_repwait_sec 1
